<Page loaded="onPageLoaded" >
	<ListView items="{{songs}}" id="lv_songs">
	 	<ListView.itemTemplate>
	 		<StackLayout>
				<Label text="{{ title }}" cssClass="title" />
				<Label text="{{ id }}" cssClass="ids" />
			</StackLayout>
		</ListView.itemTemplate>
	</ListView>
</Page>